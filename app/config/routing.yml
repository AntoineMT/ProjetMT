app:
    resource: '@AppBundle/Controller'
    type: annotation

api_articles:
    type:     rest
    prefix: api
    resource: AppBundle\Controller\ApiArticlesController

logout:
    path: /logout

index:
    path:     /
    defaults: { _controller: AppBundle:Default:index }        
      

listeuser:
    path:     /listeuser
    defaults: { _controller: AppBundle:ListeUser:listeUser }        